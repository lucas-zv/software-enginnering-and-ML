#include <iostream>
#include <cmath>

int main(){
    using std::cout;

    double x = 3.14;
    double y = 2.94;
    double z;

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = pow(x, y);
    //z = sqrt(25);
    //z = abs(-21);
    //z = ceil(x);
    //z = floor(y);

    cout << z;

    return 0;
}
